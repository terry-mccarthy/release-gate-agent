name: scenario_block_critical_lint
description: Linter returns CRITICAL severity finding — must BLOCK
diff: |
  diff --git a/app/db.py b/app/db.py
  index abc123..def456 100644
  --- a/app/db.py
  +++ b/app/db.py
  @@ -10,6 +10,7 @@ def get_user(user_id):
  +    query = f"SELECT * FROM users WHERE id = {user_id}"
  +    return db.execute(query)
mock_risk_classification:
  data_surface: low
  integration_depth: none
  vulnerability_surface: none
  rationale: "Touches database code but linter already flagged this."
lint_result:
  warnings:
    - rule: sql-injection
      message: "Raw SQL query with user-controlled input"
      severity: CRITICAL
  error_count: 1
coverage_result:
  coverage: 0.91
  lines_covered: 182
  lines_total: 200
  source: stub
expected_verdict: BLOCK
expected_check_failed: StaticAnalysisThreshold
